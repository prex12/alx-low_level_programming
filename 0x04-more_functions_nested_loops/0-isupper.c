#include "main.h"

/**
 * _isupper - checks for UPPERCASE characters in int c
 *
 * @c: int to be checked
 * Return: 1 if c is an UPPERCASE alphabet and 0 if otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
