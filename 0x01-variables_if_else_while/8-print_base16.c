#include <stdio.h>
/**
 * main - Entry point
 *
 *
 * Return: Always 0(Success)
 */
int main(void)
{
	int a;
	char c;
	/*c = (char)a; */
	for (a = 0; a < f; a++)	
		putchar(a);
	putchar("\n");
	return (0);
}
