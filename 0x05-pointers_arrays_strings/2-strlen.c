#include "main.h"

/**
 * _strlen - computes the length of string
 *
 * @s: variable holding the string value
 * Return: q
 */

int _strlen(char *s)
{
	int q;

	q = 0;
	while (s[q] != '\0')
		q++

	return (q);
}
