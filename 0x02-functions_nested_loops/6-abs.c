#include "main.h"
/**
 *
 * _abs - calculates absolute value of a given int value
 *
 * @n: checks the int
 *
 * Return: the absolute value of n
 *
 */

int _abs(int n)

{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
